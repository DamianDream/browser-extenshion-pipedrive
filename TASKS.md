# Pipedrive Fields — Задачи на разработку

## Текущая цель
Перевести расширение **Pipedrive Fields** на модель нативной боковой панели Chrome (`chrome.sidePanel`) и визуальный стиль расширения **mySmart** (`/Users/dima/Projects/mySmart`), сохранив всю функциональность управления полями Pipedrive и синхронную защиту от скачков скролла.

---

## Бэклог задач

### Фаза 1: Инфраструктура Side Panel & Manifest V3
- [x] **TASK-101**: Обновить `manifest.json`:
  - Добавить разрешение `"sidePanel"` в `permissions`.
  - Зарегистрировать фоновый service worker: `"background": { "service_worker": "background.js" }`.
  - Добавить секцию `"side_panel": { "default_path": "sidepanel.html" }`.
  - Сохранить обратную совместимость и настроить открытие боковой панели при клике на иконку.
- [x] **TASK-102**: Создать `background.js`:
  - Настроить вызов `chrome.sidePanel.setPanelBehavior({ openPanelOnActionClick: true })`.
  - Добавить обработчики `chrome.tabs.onActivated` и `chrome.tabs.onUpdated` для уведомления открытой боковой панели о смене вкладок.

---

### Фаза 2: Дизайн-система mySmart (Glassmorphism & Outfit Font)
- [x] **TASK-201**: Создать `sidepanel.css` с дизайн-токенами из mySmart:
  - Цветовая палитра:
    - Фон: `--bg-solid: #282828`, `--bg: rgba(40,40,40,0.92)`, `--bg2: rgba(48,48,48,0.70)`, `--bg3: rgba(255,255,255,0.08)`.
    - Границы: `--border: rgba(255,255,255,0.08)`, `--border2: rgba(255,255,255,0.15)`.
    - Текст: `--text: #ffffff`, `--text2: #f2f2f7`, `--text3: #ebebf5`, `--text4: rgba(235,235,245,0.6)`.
    - Акценты: `--accent: #0a84ff`, `--danger: #ff453a`, `--success: #30d158`.
  - Типографика: шрифт `Outfit`, `system-ui`, плавные градиенты, backdrop blur (`40px`).
  - Компоненты управления:
    - iOS-style toggles (плавные переключатели 32x18px с cubic-bezier).
    - Карточки групп полей со сворачиванием/разворачиванием и pill-бейджем количества полей.
    - Поле поиска с иконкой лупы и кнопкой быстрой очистки.
    - Стилизованные кнопки с эффектом наведения и нажатия.
- [x] **TASK-202**: Создать разметку `sidepanel.html`:
  - Шапка: логотип/название, статус подключения с зелёным индикатором, быстрая кнопка звука котика, красная кнопка сброса (reload), кнопка настроек.
  - Блок поиска: фильтрация полей в реальном времени.
  - Контейнер списка групп и полей: древовидная структура с поддержкой подпунктов (включая "Контактна особа").
  - Блок диалога подтверждения сброса ("Повернуть як було?" / "Хай буде так!").
  - Блок анимации загрузки (котик `cat-loader.mp4`).

---

### Фаза 3: Логика боковой панели (`sidepanel.js`)
- [x] **TASK-301**: Перенести и адаптировать логику взаимодействия с активной вкладкой:
  - Автоматическое определение активного домена Pipedrive при открытии и переключении вкладок.
  - Сканирование полей через `chrome.tabs.sendMessage(tabId, { type: 'pf-scan' })`.
  - Реактивное обновление состояния при смене вкладок (`chrome.tabs.onActivated`, `chrome.tabs.onUpdated`).
- [x] **TASK-302**: Реализовать переключение видимости групп и отдельных полей с мгновенным сохранением в `chrome.storage.local`.
- [x] **TASK-303**: Обеспечить работу управления подпунктами в группе "Контактна особа" и очистку от суффиксов `3/3 • 3`.
- [x] **TASK-304**: Интегрировать двухэтапный сброс настроек:
  - Клик на красную reload -> появление кнопок "Повернуть як було?" и "Хай буде так!".
  - Очистка правил `hidden:` и сброс состояния.
- [x] **TASK-305**: Звуковые эффекты мяуканья при клике (с кнопкой быстрого отключения в шапке и тумблером в настройках).

---

### Фаза 4: Сборка, упаковка и релиз
- [x] **TASK-401**: Поднять версию в `manifest.json` (1.1.0).
- [x] **TASK-402**: Собрать архив `pipedrive-fields.zip` для живого тестирования пользователем.

---

### Фаза 5: Редизайн хедера, настройки звука и модальный сброс правил
- [x] **TASK-501**: Очистка хедера:
  - Увеличен внутренний логотип `.brand-icon` в 2 раза (44px).
  - Убран текстовый статус ("Все правила сброшены! Все поля снова видны.") из хедера.
  - Убраны лишние кнопки сброса и звука из хедера.
  - Оставлена чистая иконка настроек `⚙` без рамок, увеличенная в 2 раза (24px).
- [x] **TASK-502**: Перенос управления звуком в настройки:
  - Чистая увеличенная иконка `🐱` / `🔇` (без контура) с возможностью проверки звука кликом.
  - Надёжный iOS-тумблер включения/выключения звука в настройках.
- [x] **TASK-503**: Двухэтапный модальный сброс правил с откатом:
  - Кнопка сброса перенесена в настройки с чистой иконкой `↺` (24px, без рамок).
  - Модальное окно подтверждения ("Сбросить все правила скрытия меню?").
  - После сброса — плашка с выбором: **«Повернуть як було?»** (полный откат) / **«Хай буде так!»** (подтверждение).
- [x] **TASK-504**: Пересборка архива `pipedrive-fields.zip`.

---

### Фаза 6: Темы оформления и радужная палитра акцентов
- [x] **TASK-601**: Добавить переключатель тем в настройки:
  - **Тёмная** (классическая dark glassmorphism `#282828`).
  - **Светлая** (чистая light glassmorphism `#f2f2f7`).
  - **Ч/Б** (монохромная высококонтрастная `#000000`).
- [x] **TASK-602**: Добавить 7 цветов радуги для акцента интерфейса:
  - 🔴 Красный (`#ff453a`)
  - 🟠 Оранжевый (`#ff9f0a`)
  - 🟡 Жёлтый (`#ffd60a`)
  - 🟢 Зелёный (`#30d158`)
  - 🩵 Голубой (`#64d2ff`)
  - 🔵 Синий (`#0a84ff`, по умолчанию)
  - 🟣 Фиолетовый (`#bf5af2`)
  - Интерактивные кружки-свотчи с индикатором выбора, мгновенное применение к тумблерам, активным элементам и сохранение в `chrome.storage.local`.
- [x] **TASK-603**: Пересборка архива `pipedrive-fields.zip`.

---

### Фаза 7: Мурчание котика при клике по логотипу
- [x] **TASK-701**: Извлечь и подключить предоставленный звук мурчания (`purr.mp3`).
- [x] **TASK-702**: Сделать логотип котика в хедере интерактивным (`role="button"`, подсказка «Погладить котика», курсор-рука, анимация вибрации котика при мурчании).
- [x] **TASK-703**: При клике запускать мурчание, при повторном нажатии перезапускать звук с начала.
- [x] **TASK-704**: Пересобрать архив `pipedrive-fields.zip`.

---

### Фаза 8: Умеренный гласморфизм (Эксперимент с мгновенным откатом)
- [x] **TASK-801**: Создать контрольную точку отката (`git checkpoint: pre-glassmorphism` и резервная копия `extension.backup-pre-glass/`).
- [x] **TASK-802**: Реализовать умеренный стеклянный эффект (матовое стекло, `backdrop-filter: blur`, полупрозрачные многослойные градиенты, скошенные световые блики 1px, мягкие тени) для хедера, карточек групп, панели настроек, строки поиска и бейджей.
- [x] **TASK-803**: Обеспечить 100% читаемость и контрастность текста на темной подложке.
- [x] **TASK-804**: Пересобрать `pipedrive-fields.zip`, синхронизировать в папку outputs.

---

### Фаза 9: Утверждение гласморфизма и удаление переключателя тем
- [x] **TASK-901**: Гласморфизм принят пользователем на постоянную основу.
- [x] **TASK-902**: Полностью удалён переключатель тем из настроек (`sidepanel.html`, `sidepanel.js`, `sidepanel.css`). Тёмная тема зафиксирована всегда.
- [x] **TASK-903**: Сохранена радужная палитра акцентов (7 цветов) и поддержка их мгновенного переключения.
- [x] **TASK-904**: Очищен устаревший ключ `pf-theme` из хранилища и удалены временные бэкапы.
- [x] **TASK-905**: Пересобран `pipedrive-fields.zip` и синхронизирован с рабочей папкой.


